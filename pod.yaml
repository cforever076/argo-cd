apiVersion: v1
kind: Pod
metadata:
  name: nginx-argocd-pod
  namespace: dev
  labels:
    app: dev
spec:
  containers:
    - name: nginx-argocd-container
      image: nginx
      ports:
        - containerPort: 80
