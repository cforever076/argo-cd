# apiVersion: v1
# kind: Pod
# metadata:
#   name: nginx-argocd-pod
#   labels:
#     app: dev
# spec:
#   containers:
#     - name: nginx-argocd-container
#       image: nginx
#       ports:
#         - containerPort: 80
